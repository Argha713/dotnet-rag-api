server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # Argha - 2026-03-01 - SPA fallback: return index.html for all Blazor deep-linked routes
        try_files $uri $uri/ /index.html;
    }
}
